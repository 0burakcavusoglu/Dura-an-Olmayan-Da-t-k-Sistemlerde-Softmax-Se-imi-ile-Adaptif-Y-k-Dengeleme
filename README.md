Multi-Armed Bandit Tabanlı Akıllı Yük Dengeleyici (Softmax)Bu proje, Algoritma Analizi ve Tasarımı dersi kapsamında, dağıtık sistemlerde yük dengeleme (load balancing) problemini çözmek amacıyla geliştirilmiştir. Geleneksel yöntemlerin aksine, sunucu performanslarının zamanla değiştiği (non-stationary) ve gürültülü olduğu senaryolarda Softmax Action Selection algoritmasının performansı analiz edilmiştir. Problem TanımıDağıtık bir sistemde $K$ adet sunucu bulunmaktadır. Bu sunucuların yanıt süreleri (latency) sabit değildir ve zamanla değişkenlik gösterir. Amaç, toplam bekleme süresini minimize edecek (ödülü maksimize edecek) bir istemci taraflı yük dengeleme algoritması tasarlamaktır.Karşılaştırılan Algoritmalar:Round-Robin: Sunucuları sırayla seçer, performansı dikkate almaz.Random Selection: Sunucuları tamamen rastgele seçer.Softmax Action Selection: Geçmiş performans verilerine dayanarak olasılıksal bir seçim yapar. Algoritma Mantığı (Softmax)Softmax algoritması, her sunucuya bir "seçilme olasılığı" atar. Bir sunucunun gecikme süresi ne kadar düşükse (ödülü ne kadar yüksekse), seçilme ihtimali o kadar artar.Olasılık hesabı şu formüle dayanır:$$P_t(a) = \frac{e^{Q_t(a) / \tau}}{\sum_{i=1}^{K} e^{Q_t(i) / \tau}}$$$\tau$ (Temperature): Keşif (exploration) ve faydalanma (exploitation) arasındaki dengeyi belirler.$Q_t(a)$: Sunucunun o ana kadar tahmin edilen performans değeridir.🛠️ Kurulum ve ÇalıştırmaProjeyi klonlayın:Bashgit clone https://github.com/kullaniciadi/proje-adi.git
Gerekli kütüphaneleri yükleyin:Bashpip install numpy matplotlib
Simülasyonu çalıştırın:Bashpython load_balancer.py
 Analiz SonuçlarıYapılan simülasyon sonucunda, Softmax algoritmasının zamanla en hızlı sunucuları "keşfettiği" ve statik olmayan (sunucu hızlarının değiştiği) ortamlarda Round-Robin ve Random yöntemlerine göre çok daha düşük ortalama gecikme sağladığı gözlemlenmiştir.
